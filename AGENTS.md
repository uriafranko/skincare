# Agent Instructions

- Only create pull requests against our repository: `uriafranko/skincare`.
- Do not create pull requests against the upstream repository, `pontusab/caltext`, unless the user explicitly asks for that exact target.
- When using GitHub CLI, pass `--repo uriafranko/skincare` for PR operations to avoid defaulting to upstream.
